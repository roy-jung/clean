export default class {
    constructor (id, name, address, classification, schedule, method) {
        this.empId = id;
        this.name = name;
        this.address = address;
        this.classification = classification;
        this.schedule = schedule;
        this.method = method;
    }
}
